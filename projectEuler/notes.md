#cram THE PANDIGITAL CHECK GIVEN IN 44.CPP

use `#include<bits/stdc++.h >` instead of writing all other `#include` s
use double, ceil and floor together in order to increase precision of floating point see 45.cpp
